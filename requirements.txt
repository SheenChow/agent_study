# Agent Web 应用依赖
# 安装方式: pip install -r requirements.txt

# Web框架
flask>=3.0.0

# 阿里云DashScope SDK (用于调用千问模型)
dashscope>=1.10.0

# 用于读取环境变量
python-dotenv>=1.0.0

# HTTP请求库（用于搜索API）
requests>=2.28.0

# OpenAI SDK (可选，用于OpenAI模型)
# openai>=1.0.0
